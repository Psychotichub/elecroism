# Optional Sentry DSN for opt-in crash reporting (Privacy & diagnostics in Window menu).
# Leave unset in local/dev builds — reports stay in a local offline queue.
# VITE_SENTRY_DSN=https://<key>@o<org>.ingest.sentry.io/<project>
# VITE_APP_VERSION=0.0.0
